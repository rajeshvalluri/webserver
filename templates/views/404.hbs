<!DOCTYPE html> 
<html>

<head>
    <title>{{title}}</title>
    <link rel="icon" href="/img/weather.png">
    <link rel="stylesheet" href="./css/styles.css">
    <body> 
        <div class="main-style">
        {{>header}}
        <h1> {{errorMessage}} </h1>
        </div>
        {{>footer}}
    </body>

</head>

</html>